import { Button } from "@/components/ui/button";

const Logout = () => {
  return (
    <Button className="cursor-pointer px-2 py-1 bg-red-500">Logout</Button>
  );
};

export default Logout;
